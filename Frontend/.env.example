# Optional: set this in production builds if frontend cannot proxy /api to the backend.
# Leave empty in dev (Vite proxy handles /api -> http://127.0.0.1:8001)
VITE_API_BASE_URL=http://127.0.0.1:8001
